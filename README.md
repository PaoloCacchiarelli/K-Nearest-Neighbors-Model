# K-Nearest-Neighbors-Model

KNN is a supervised machine learning algorithm that classifies or predicts data based on the majority of its nearest neighbors. It operates on the premise that similar points are located close to each other and can be used for classification tasks (majority voting among neighbors) and regression (averaging the values ​​of neighbors).

It was used with the Iris and Titanic datasets.
